interface:
  display_name: "Module Notes Maintainer"
  short_description: "Maintain module/function notes and runbooks"
  default_prompt: "Use $module-notes-maintainer to update module_notes by module/function, preserve reusable commands in runbook.md, keep module_notes/README.md as the only index, and run the audit script after migrations or broad cleanup."
